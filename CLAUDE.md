# ABC Universe — Telegram Mini App + amoCRM — контекст для Claude

Читай этот файл первым, если тебе передали эту папку без предыстории. Он
описывает, что это за проект, что уже сделано, что развёрнуто и работает
прямо сейчас, и что осталось доделать. Инструкция «с нуля» — в `SETUP.md`,
но если проект уже настроен (а он настроен), сюда смотри в первую очередь.

## Что это

Telegram Mini App для студентов ABC Universe (программа Work & Travel /
au pair и т.п.): показывает участнику его текущий этап оформления
(roadmap), документы, платежи, брифинги, мероприятия. Данные приходят из
amoCRM (сделка участника = его карточка), синхронизируются в Google Sheets
через Google Apps Script, который выполняет роль backend — отдельного
сервера (Node/Python) нет и сознательно не нужно, см. раздел
«Ограничения» в конце `SETUP.md`.

Плюс несколько служебных HTML-страниц для координаторов (не для студентов):
`admin.html`, `admin-events.html`, `admin-users.html` — они открываются
координатором в браузере отдельно от Mini App, но используют тот же
backend и тот же `ADMIN_SECRET`.

## Архитектура одним взглядом

```
Telegram (Mini App)  ──┐
admin*.html (браузер) ──┼──►  Google Apps Script (Web App)  ◄──►  Google Sheets (БД)
                        │              │  │
                        │              │  └──►  Wazzup24 API (WhatsApp внутри amoCRM)
                        │              └─────►  amoCRM REST API v4 (сделки участников)
                        │
   фронтенд — статика (index.html + js/ + css/), хостится на GitHub Pages
```

- Фронтенд Mini App: `index.html`, `js/`, `css/` — чистый JS без сборки,
  ES-модули напрямую в браузере. `js/services/config.js` содержит
  `BACKEND_URL` — единственное, что связывает фронтенд с backend. Если
  `BACKEND_URL` пустой — приложение работает на mock-данных
  (`js/services/mockApi.js`), это Phase 1/демо-режим; сейчас он заполнен,
  значит приложение уже «живое» (`js/services/liveApi.js`).
- Backend: папка `backend-apps-script/` — 9 файлов `.gs`, живут **не**
  в этом гите как исполняемый код, а как зеркало того, что вручную
  скопировано в реальный проект Google Apps Script (см. «Живая
  инфраструктура» ниже). Это важно: **правки в `backend-apps-script/*.gs`
  в этом репозитории сами по себе ничего не меняют** — их нужно ещё
  руками (через браузер, script.google.com) перенести в реальный
  Apps Script проект и передеплоить. Как это делать — см. раздел ниже.
- БД: Google Sheets, схема — `SHEET_SCHEMA` в `backend-apps-script/Setup.gs`.
- Координаторские инструменты (`admin.html` и т.п.) — статические файлы,
  хостятся вместе с Mini App на том же GitHub Pages, но открываются
  координатором напрямую по URL, не через Telegram. Вход — по паролю
  `ADMIN_SECRET`, хранится в localStorage браузера координатора.

## Живая инфраструктура (актуально на момент этой записи)

- **GitHub-репозиторий**: `https://github.com/9700625-bit/wtusa-self-app`
  (приватный или публичный — проверь на месте). Хостинг фронтенда —
  GitHub Pages от этого же репозитория.
- **Apps Script проект**: открывается по адресу
  `https://script.google.com/u/0/home/projects/1PH0pA80_Jz4pZSI9vaP399S4KDjkm2qIDIRieukWxFNvNTC7G9-bLCeZ/edit`
  (это отдельный от гита «живой» код — см. предупреждение выше).
- **Web App URL** (он же `BACKEND_URL` в `js/services/config.js`):
  `https://script.google.com/macros/s/AKfycbxCe_iRK4uEJIw3t-d7Iypj7QbMZTw9M0rHj6UbgRo4dyV2kuJN1d4dH1kln1h1apIZjA/exec`
  — этот URL стабилен между деплоями (меняется только при создании
  *нового* Web App деплоя с нуля, а не при обновлении версии
  существующего), менять его во фронтенде обычно не требуется.
- Локальная копия репозитория на компьютере пользователя (если сессия
  подключена через remote-devices/desktop bridge): обычно
  `~/Desktop/Development/MiniApp` — но не считай это железным фактом,
  спроси/проверь, если бридж подключён.
- Google Sheet (БД) и Telegram-бот — их ID/username не хранятся в этом
  репозитории (это Script Properties, см. ниже), спроси пользователя или
  посмотри в Apps Script → Project Settings → Script Properties, если
  нужен доступ.

## Секреты (Script Properties в Apps Script — НЕ в гите)

Все значения живут в Apps Script → Project Settings (шестерёнка) →
Script Properties. В коде — только имена ключей (`CFG("...")` в
`Config.gs`), реальные значения никогда не коммитятся. Полный список с
пояснениями — шапка комментария `Config.gs`. Коротко:

Обязательные: `TELEGRAM_BOT_TOKEN`, `TELEGRAM_BOT_USERNAME`,
`AMO_SUBDOMAIN`, `AMO_ACCESS_TOKEN`, `WEBHOOK_SECRET`, `SHEET_ID`,
`ADMIN_SECRET`.

Условно обязательные: `AMO_CLIENT_ID`/`AMO_CLIENT_SECRET`/
`AMO_REDIRECT_URI`/`AMO_REFRESH_TOKEN` — нужны, только если используется
обычный OAuth-обмен токена amoCRM. Если вместо этого выписан
«долгосрочный токен» (Долгосрочный токен, ~1 год жизни) прямо в
Ключи и доступы amoCRM — можно оставить только `AMO_ACCESS_TOKEN`, эти
четыре не нужны (см. комментарий над `getAmoAccessToken_()` в
`AmoCRM.gs`). **Так это сейчас и настроено** — не считай их отсутствие
багом, если увидишь через диагностику.

Опциональные: `WAZZUP_API_KEY`, `WAZZUP_CHANNEL_ID` (WhatsApp через
Wazzup24, см. ниже), `LINK_TOKEN_TTL_HOURS`, `STATUS_ID_MAP_JSON`,
`FIELD_ID_*` (числовые ID кастомных полей amoCRM).

## Что сделано / текущий статус

Проект полностью настроен и работает (не MVP-набросок): Mini App
подключён к реальным данным amoCRM через Google Sheets, вебхуки
синхронизации работают, напоминания по расписанию настроены.

Последнее по времени: добавлена отправка сообщения со ссылкой на
привязку участника через **WhatsApp внутри amoCRM** (через API Wazzup24 —
у координаторов уже настроен виджит Wazzup в imBox amoCRM, поэтому не
стали делать отдельную кнопку wa.me, которая увела бы переписку из CRM) —
новый файл `backend-apps-script/Wazzup.gs` + действия `adminSendWhatsApp`
и `adminListAllParticipants` в `Api.gs`. Плюс новая страница
`admin-users.html` — координаторский дашборд «кто уже подключился к
приложению» (список участников с последней активностью, метка «давно не
заходил»).

Это уже задеплоено. Актуальный деплой — **Version 37** (03.09.2026):
в него вошли правки аудита, сухие тексты уведомлений по этапам и новый
порядок этапов CIEE. Предыдущий деплой был Version 36 (проверено
рабочим тестовым запросом на `adminListAllParticipants`/
`adminSendWhatsApp` — оба отвечают корректной ошибкой на неверный секрет,
значит роуты подключены). Диагностика Script Properties и данных Sheets
(261 участник, 8 привязаны к Telegram, 253 непривязанные сделки из
amoCRM, токен amoCRM живой) прогонялась и прошла чисто.

## Что осталось доделать (по приоритету)

1. ~~`WAZZUP_API_KEY` не добавлен~~ — **сделано 08.09.2026.** Пользователь
   получил ключ в Wazzup24 и вписал его в Script Properties сам (я
   специально не ввожу API-ключи/токены в поля даже когда их продиктовали —
   это моё общее правило безопасности).
2. ~~`WAZZUP_CHANNEL_ID` не найден~~ — **сделано 08.09.2026.** Запустили
   `listWazzupChannels()` (`Wazzup.gs`) через «Выполнить», в Журнале
   выполнения нашли канал с `"transport": "whatsapp"`, `"state": "active"`
   (`channelId: 4f06e45f-99d0-4c9a-98da-0acbb0f1b2b1`) и добавили его как
   Script Property `WAZZUP_CHANNEL_ID`. Интеграция Wazzup настроена
   полностью, новый деплой не требовался.
   **Проверено end-to-end 08.09.2026.** С разрешения пользователя (живое
   подтверждение в чате, не по инструкции из фонового триггера) отправлено
   реальное тестовое сообщение через `sendWazzupWhatsApp_()` на номер
   +7 913 669-97-27 (контакт «Andrey» в amoCRM). Вызов сделан вручную из
   редактора Apps Script (кнопка «Выполнить» на временной функции
   `testWazzupSend_Andrey()` в `Setup.gs`, потом удалена) — это специально
   в обход HTTP-роута `adminSendWhatsApp`, чтобы не вводить `ADMIN_SECRET`
   самому (моё правило безопасности). Журнал выполнения показал успешный
   ответ Wazzup: `{"messageId":"23a6b47f-48ba-4a22-acb8-9af0b9889157",
   "chatId":"79136699727"}`, без ошибок. Доставляемость подтверждена.
3. ~~Новая версия развёртывания~~ — **сделано 03.09.2026, Version 37.**
   Здесь важен урок на будущее, а не сам факт. Диалог «Deploy → Manage
   deployments» падал с «Something went wrong / Перезагрузите страницу», и
   это выглядело как сбой Google. На самом деле красная плашка ЗАКРЫВАЛА
   собой настоящий текст ошибки, который лежал в диалоге под ней:
   **«You do not have permission to perform this action»**. Если увидишь
   такую плашку — не перезагружай страницу по кругу, а вычитай текст
   диалога из DOM, там будет причина.
   Причина же в аккаунтах: **проект принадлежит `9700625@gmail.com`
   (ABC Universe LLC), а браузер по умолчанию открывает его под
   `d.gorelova@business-mission.org`**. Редактировать и сохранять код
   может не-владелец, а создавать развёртывания — только владелец.
   Лечится сменой номера аккаунта в URL: `/u/0/` → **`/u/1/`**
   (проверь, какой номер у нужного аккаунта — Apps Script показывает
   «You're currently signed in as …» при открытии). Тот же аккаунт
   `9700625` владеет и GitHub-репозиторием.
   Развёртывание обновляется так: Deploy → Manage deployments →
   карандаш → Version: **New version** → Deploy. Deployment ID и URL при
   этом НЕ меняются — проверено, `BACKEND_URL` остался прежним.

4. ~~Уведомление о принятом платеже~~ — **сделано 04.09.2026.** Координатор
   переводит платёж в «Оплачено» в amoCRM → `syncPaymentsFromDeal_` /
   `syncVariablePayment3_` в `Webhooks.gs` ловят переход
   `existing.status !== "paid" && status === "paid"` и шлют студенту через
   новую `notifyPaymentPaid_(participant, label)` сообщение «✅ Платёж
   получен» (без суммы — ограничение проекта в силе). Задеплоено как
   Version 38 (04.09, 17:30), сейчас активна более поздняя Version 39 —
   она включает этот код. Этот файл (CLAUDE.md) просто не был обновлён
   вовремя; если увидишь этот пункт снова «не начатым» — сначала проверь
   живой `Webhooks.gs`, а не верь на слово документации.
5. ~~Порядок статусов CIEE в воронке amoCRM~~ — **сделано 08.09.2026.**
   В воронке «Сопровождение SELF» (pipeline_id 9881242) статусы
   «Проверка Сiee анкеты» (id 87245618) и «Заполнен личный кабинет»
   (id 78615418) стояли в старом порядке — поменяны местами через
   amoCRM REST API (`sort`: 60/50). Перетаскивание колонок в UI настроек
   воронки не работает через браузерную автоматизацию — используй
   `fetch('/api/v4/leads/pipelines/{id}/statuses/{status_id}', {method:
   'PATCH', credentials:'include'})` прямо в консоли залогиненной вкладки.
   **Ловушка:** PATCH без поля `name` стирает название статуса (амоCRM
   трактует его как замену объекта, не частичный patch) — всегда передавай
   `name` вместе с `sort`.

**Про `git push`.** Раньше здесь было написано, что пуш может сделать
только пользователь. Это осталось верным для `git` (учётных данных нет
ни в облаке, ни в оболочке бриджа), но 03.09.2026 репозиторий был
приведён в актуальное состояние **через веб-интерфейс GitHub** —
браузер пользователя авторизован как владелец, и «Add file → Upload
files» коммитит без всяких токенов. Это рабочий обходной путь, если
`git` на машине недоступен. Учти при этом две вещи: загрузка идёт по
одной папке за коммит (у input нет `webkitdirectory`), а кнопку
«Commit changes» надёжнее нажимать через `form.requestSubmit(btn)` —
обычный клик по координатам на этой странице срабатывает не всегда.

Отдельно: у пользователя `git` в Терминале не видел папку на Desktop
(`fatal: Unable to read current working directory: Operation not
permitted`) — это macOS TCC, а не поломка репозитория; лечится выдачей
Терминалу доступа к папке Desktop в «Конфиденциальность и безопасность
→ Файлы и папки» с последующим полным перезапуском Терминала.

## Сквозной тест кода (09.09.2026)

По просьбе владельца прогнан полный тест «чтобы не было ошибок и всё
работало», после того как GitHub был приведён в актуальное состояние
(см. выше). Результат:

- **Синтаксис**: все 11 файлов `backend-apps-script/*.gs` и все 25
  файлов `js/**/*.js` — `node --check` без ошибок (файлы `js/` тянулись
  с GitHub, а не с диска владельца, см. ниже почему).
- **Живой бэкенд**: `?action=state` с пустым `initData` отвечает HTTP
  200 и корректным `{"error":"UNAUTHORIZED: invalid or missing Telegram
  initData"}` — правильное поведение вне Telegram, не падение сервера.
  `?action=adminListAllParticipants` с заведомо неверным `secret`
  отвечает чистым `{"error":"bad secret"}` — роут подключён, код цел
  (это тот самый безопасный способ проверки без ввода настоящего
  ADMIN_SECRET, см. раздел «Тестирование / диагностика» ниже).
- **Живой фронтенд** (GitHub Pages, `https://9700625-bit.github.io/wtusa-self-app/`):
  открывается без JS-краша; единственная ошибка в консоли —
  ожидаемый `UNAUTHORIZED` при открытии вне Telegram, и приложение
  корректно показывает пользователю плашку «Нужно переоткрыть
  приложение» вместо белого экрана. `admin.html` показывает форму
  входа без ошибок.
- **Согласованность STAGE_IDS** между `Config.gs` (бэкенд) и
  `roadmap.config.js` (фронтенд) — при сверке найдена и исправлена
  серьёзная ошибка отката, см. запись про `124c1df` выше.

**Важное техническое наблюдение**: часть файлов `js/` (примерно 19 из
25 — `app.js`, `router.js`, большинство `screens/*` и `services/*`) на
диске владельца оказались Dropbox-заглушками (`cloudOnly`, online-only,
не скачаны локально) — `device_bash` падал на них с ошибкой `Resource
deadlock avoided`, а автостейджинг через bridge отказывался их тянуть
(«Open it in Finder to download it first» — в отличие от iCloud,
Dropbox online-only файлы бридж не гидрирует сам). Поэтому синтаксис
фронтенда проверялся по копиям с GitHub, а не с диска. Если в будущем
понадобится реально редактировать эти файлы на диске (не просто
читать/тестировать то, что уже в гите) — сначала попросить владельца
открыть их в Finder/Dropbox, чтобы скачать (или включить постоянную
локальную копию папки в настройках Dropbox), иначе device_bash и
device_stage_files будут на них падать.

## Повторный самоаудит и найденный пробел с триггером (09.09.2026)

По просьбе владельца («перепроверь сам себя, в тестах, в отправках
сделок, уведомлениях итд») повторно и более скептически проверена вся
цепочка уведомлений и фоновых задач:

- **Цепочка уведомлений о смене этапа сделки** прослежена целиком и
  подтверждена рабочей: вебхук amoCRM → `doPost` → `Webhooks.gs` →
  `syncDealToSheets` → при смене этапа (`newStageId !== oldStageId`) →
  `STAGE_NOTIFY_TEXT[newStageId]` → `notifyParticipantByDealId` →
  Telegram Bot API. Уведомление об оплате идёт отдельным защищённым
  путём (`notifyPaymentPaid_`, сравнение старого/нового статуса, чтобы
  не слать дубли на каждый вебхук).
- **Wazzup/WhatsApp** (`sendWazzupWhatsApp_` в `Wazzup.gs`) подтверждён
  доступным только через админский HTTP-роут в `Api.gs`
  (`adminSendWhatsApp`, под `ADMIN_SECRET`) — НЕ подключён ни к
  какому автоматическому потоку (ни к смене этапа, ни к оплате). Если
  это не то поведение, что задумано, — нужно отдельное решение
  владельца, доделывать не стал без разрешения.
- **Живой бэкенд**: 0 реальных сбоев в трафике `doPost`/`doGet` за
  последние 7 дней (только 2 старых, несвязанных ручных сбоя от
  04.09).
- **Батч-1 перемещения сделок** (8 из «Приглашение презентация» →
  «Были на презентации») подтверждён живым запросом к amoCRM API —
  реально применился (`status_id: 88209758` у всех 8). Оставшиеся 9
  сделок сознательно не трогались — по прямому указанию владельца
  «сделки не трогай сейчас».

**Найден реальный, ранее не замеченный пробел**: в проекте Apps Script
было установлено **0 триггеров** (страница Триггеры показывала
«Показано триггеров: 0»). Нигде в коде нет `ScriptScript.newTrigger()`
(проверено grep-ом) — значит `sendScheduledReminders()`
(`Reminders.gs`, напоминания об оплате за 7/3/0 дней + просрочка, и
эскалация по 5-дневному дедлайну активации CIEE) никогда не
запускалась автоматически, только если кто-то вручную нажимал «Run» в
редакторе. Это не связано с моей более ранней ошибкой отката
Config.gs/Setup.gs — отдельный, самостоятельно найденный пробел.

**Исправлено** (после явного разрешения владельца «давай чинить
приложение сделки не трогай сейчас»): в Apps Script → Триггеры создан
триггер: функция `sendScheduledReminders`, развёртывание «Основное
развёртывание», источник события «Триггер по времени», тип «По дням»,
время «С 0:00 до 1:00» (GMT+05:00 — тихий час, никто не пользуется
приложением), уведомления об ошибках — «Каждый день» (по умолчанию).
Проверено дважды (после сохранения и после полной перезагрузки
страницы) — «Показано триггеров: 1», владелец «Я», событие «По
времени», функция `sendScheduledReminders`.

`sendPendingEventInvites()` (`Events.gs`) триггер не требуется —
координатор запускает вручную и намеренно (комментарий в коде: «Safe
to re-run»).

## Платёж 3: автоматический дедлайн +4 месяца (09.09.2026)

По договору сроки оплаты: платёж 1 — 5 дней, платёж 2 — 30 дней,
платёж 3 — 4 месяца с момента создания сделки. Платежи 1 и 2 уже
считались автоматически (`FIXED_PAYMENTS_` в `Webhooks.gs`, без
изменений). Платёж 3 же был полностью ручным — ни дедлайна, ни суммы
по умолчанию не было, координатор вводил дату дедлайна вручную в поле
FIELD_ID_PAY3_DEADLINE в amoCRM, либо не вводил вообще (0 триггеров /
0 расчёта — реальные дедлайны платежа 3 никогда не выставлялись
автоматически).

Пользователь подтвердил: оставить 5 и 30 дней как есть, добавить
автоматический расчёт дедлайна платежа 3 (+4 месяца от даты создания
сделки) для всех студентов по умолчанию. Индивидуальная договорённость
— исключение: если координатор вручную указал дату в
FIELD_ID_PAY3_DEADLINE на карточке сделки, она перекрывает
автоматический расчёт (`manualDeadline || autoDeadline` в
`syncVariablePayment3_`).

Изменение внесено в `backend-apps-script/Webhooks.gs`, синхронизировано
во все три копии (диск / живой Apps Script / GitHub-зеркало,
коммит `34a7ad3`). Сумма платежа 3 (`FIELD_ID_PAY3_AMOUNT`) по-прежнему
вводится координатором вручную — только дедлайн стал автоматическим.

## Решения по «интерфейс обещает то, чего нет» (03.09.2026)

Аудит происхождения данных нашёл три места, где приложение показывало
студенту выдуманное. Все три разобраны, решения приняты владельцем:

1. **Комментарий координатора на `JOB_PROBLEM`** — в `roadmap.config.js`
   лежал текст «Работодателю необходимо исправить даты работы», один и
   тот же для всех. Убран. Сам этап-ветка `JOB_PROBLEM` сохранён целиком
   (кнопка «Написать координатору», баннер на «Пути», уведомление) —
   решение было «убрать текст, ветку оставить». Настоящего источника у
   комментария нет: `coordinator_comment` есть только в листе `Documents`,
   а Job Offer отдельным документом не заведён. Чтобы показывать реальную
   причину, понадобится новое поле в amoCRM + колонка в `Participants` +
   синк в `Webhooks.gs`; рендер в `statusDetail.js` для этого уже готов и
   срабатывает, как только `stage.coordinatorComment` непустой.

   **Сделано (08.09.2026).** Реальный источник заведён целиком:
   amoCRM-поле сделки «Комментарий по Job Offer (для студента)» (id
   `1381231`, тип `textarea`, в пайплайне «Сопровождение self») →
   Script Property `FIELD_ID_JOB_PROBLEM_COMMENT` (Config.gs) → колонка
   `job_problem_comment` в листе `Participants` → `Webhooks.gs`,
   `syncDealToSheets()` кладёт `fieldGet("FIELD_ID_JOB_PROBLEM_COMMENT")`
   при каждом вебхуке по сделке → `Api.gs`, `stateForUser_()` отдаёт его
   на фронтенд как `participant.jobProblemComment` (`null`, если пусто)
   → `js/services/deriveViews.js`, `deriveStageDetail()` кладёт его в
   `stage.coordinatorComment` только для `stageId === "JOB_PROBLEM"` и
   только когда значение непустое → `statusDetail.js` рендерит карточку
   без изменений (код был готов заранее). Координатор пишет текст прямо
   в это поле amoCRM на сделке — больше нигде трогать не нужно. Живой
   Apps Script (Webhooks.gs и Api.gs) отредактирован и задеплоен новой
   версией — эта часть уже в проде.

   **Пуш на GitHub сделан 09.09.2026, с одной серьёзной ошибкой по пути —
   держать в голове на будущее.** 08.09 браузерная вкладка была
   разлогинена (видны «Sign in» / «Sign up»); залогинились под чужим
   GitHub-аккаунтом (`andrei897` вместо `9700625-bit`) — «Uploads are
   disabled» на странице загрузки значит именно «нет прав push», а не
   «вкладка разлогинена вовсе»; проверяется через аватар → открывшееся
   меню (имя аккаунта), НЕ по хлебным крошкам репозитория — они всегда
   показывают владельца репо, а не текущего пользователя.

   После входа под `9700625-bit`: сравнил 5 файлов disk vs GitHub по
   sha256 (`Api.gs`, `Config.gs`, `Setup.gs`, `Webhooks.gs`,
   `js/services/deriveViews.js`) и **ошибочно решил, что раз хеши не
   совпадают — значит диск владельца новее везде**, и залил все 5 файлов
   с диска поверх GitHub одним махом. Это было верно для `Api.gs`,
   `Webhooks.gs`, `deriveViews.js` (там диск действительно был новее —
   фича `jobProblemComment` от 08.09), но **неверно для `Config.gs` и
   `Setup.gs`**: у владельца на диске эти два файла оказались СТАРЕЕ
   GitHub — там не было переработки воронки Self от 04-07.09.2026
   (новые статусы `JOB_OFFER_SENT_INTL_REP` → ... → `JOB_OFFER_CIEE_FINAL_REVIEW`,
   `PLACEMENT_COMPLETED`, `VISA_INTERVIEW_SCHEDULED`, функция
   `wireUpSelfPipelineMapping2()`). Коммит `57fb3f9` откатил оба файла на
   старую версию — молча, без предупреждения, потому что я не сверил
   направление diff с тем, что сам же документировал в этом файле
   несколькими абзацами ниже.

   Поймано в тот же день при сквозном тесте кода (см. следующий раздел):
   для `Config.gs`/`Setup.gs` diff должен был читаться в обратную
   сторону от того, что я предположил. Восстановлено коммитом `124c1df`
   («Revert: restore Self pipeline rework») из истории GitHub — взял
   содержимое файлов на коммитах `d980441` (Config.gs) и `514d37f`
   (Setup.gs), это последние версии ПЕРЕД моей ошибкой. Проверено: обе
   фичи (Self-переработка И jobProblemComment) теперь сосуществуют
   в репозитории корректно.

   **Урок**: перед тем как решать, чей файл «новее» при расхождении
   diff/hash, сверяться с уже написанной документацией в этом файле
   (даты, описания фич), а не полагаться на «раз отличается — значит
   у меня актуальнее». В этом случае Config.gs/Setup.gs и
   Api.gs/Webhooks.gs/deriveViews.js разошлись в РАЗНЫЕ стороны
   одновременно — по разным причинам (одни правились на диске позже,
   другие раньше и только в live-редакторе/через отдельный upload) —
   и это нормально для воркфлоу с ручным зеркалированием, не считать
   «всё с одной стороны новее» само собой разумеющимся.

   Заодно подтверждено: этот инцидент НЕ затронул реальный работающий
   бэкенд — `backend-apps-script/*.gs` в гите лишь зеркало для истории,
   живой код исполняется в отдельном Apps Script проекте и правится там
   вручную (см. «Как редактировать backend» ниже), его я не трогал.
2. **`reminderSchedule` на `VISA_FINAL_CALL`** — карточка «Напоминания в
   Telegram» обещала 4 напоминания перед визовым интервью. Такой рассылки
   в `Reminders.gs` нет и не было (там только `sendPaymentReminders_` и
   `sendCieeActivationReminders_`). Удалены обе части — поле в конфиге и
   рендер в `statusDetail.js`. Возвращать только вместе с реальной
   рассылкой и time-driven триггером. Дата интервью и обратный отсчёт на
   этом экране настоящие, они из `VisaInfo`.
3. **`Participants.name` — это название сделки amoCRM**, а не имя
   человека (кладёт `syncParticipantFromDeal_` в `Webhooks.gs`). Имя
   убрано из приветствия на главной: теперь просто «Добрый день 👋».
   Полное ФИО есть в `full_name`, но заполнено не у всех; если захочется
   вернуть обращение по имени — заводить в amoCRM отдельное поле «имя для
   обращения», а не разбирать ФИО и не использовать `name`.

Общее правило, которое из этого следует: **в `roadmap.config.js` не место
данным, которые выглядят как персональные.** Конфиг этапов — это копия
интерфейса, одинаковая для всех; всё, что должно отличаться у разных
студентов, приходит из Sheets через `Api.gs`.

## Фикс STATUS_ID_MAP_JSON и новый этап SUBMIT_TO_CIEE (10.09.2026)

По просьбе владельца («давай сделаем») исправлена устаревшая
`STATUS_ID_MAP_JSON` (Script Property, задаётся функцией
`wireUpSelfPipelineMapping()` в `Setup.gs`) и синхронизирован
`js/config/roadmap.config.js` под реальную воронку amoCRM.

- Перепрогнан `listAmoPipelineStatuses()` (диагностическая функция в
  `Setup.gs`) вживую против воронки «Сопровождение self»
  (pipeline_id `9881242` — не перепутать с похожей по названию
  «Сопровождение full», id `9881246`, тоже есть в аккаунте). Получены
  все 20 актуальных `status_id`, включая статусы переработки
  job_offer/visa от 04.09.2026 (`JOB_OFFER_SENT_INTL_REP` и далее) —
  они подтверждены реально существующими, это была не ошибка отката, а
  финальная сверка.
- `wireUpSelfPipelineMapping()` обновлена (актуальная карта из 20
  записей) и **выполнена вручную** через «Выполнить» в редакторе —
  `STATUS_ID_MAP_JSON` и `AMO_PIPELINE_ID` (`9881242`) установлены как
  Script Properties в живом проекте. Подтверждено логом выполнения.
- **Найдена вторая, ранее неизвестная функция** `wireUpSelfPipelineMapping2()`
  (строки ~274-300 в живом `Setup.gs`), явно созданная другой/более
  ранней сессией около 09.09.2026 — почти дубликат, но без
  `SUBMIT_TO_CIEE` (19 записей вместо 20). Не тронута и не выполнена —
  кандидат на удаление, но это на усмотрение владельца, сама по себе
  не вредит (мёртвый код, ничего не вызывает).
- Добавлен новый этап **`SUBMIT_TO_CIEE`** (роадмап + новая
  одноимённая колонка в amoCRM) между `PLACEMENT_COMPLETED` и
  `DS2019_ISSUED` — раньше отправка документов в CIEE была невидимым
  шагом.
- `js/config/roadmap.config.js` пересобран **с нуля от актуального
  содержимого GitHub** (а не от локального черновика на диске
  пользователя, который местами устарел и разошёлся с тем, что уже
  было запушено 04.09-09.09) — поверх него применены точечные правки
  текстов, согласованные с владельцем построчно в чате: ENROLLED
  (текст без привязки к оплате), несколько описаний в группах Job
  Offer/Visa, переименование `JOB_OFFER_PARTICIPANT_REVIEW` →
  «Participant review» с повышением severity до `danger`, плюс новый
  `SUBMIT_TO_CIEE`. Все 20 `id` в файле теперь 1:1 совпадают с ключами
  `STATUS_ID_MAP_JSON`. Запушено на `main` через веб-редактор GitHub,
  коммит `226a83c`.
- Инструкция-кнопка «Как загрузить офер в кабинете» на `CIEE_FILLED` и
  видео-инструкция для `DS160_STARTED` — намеренно оставлены без
  `cta`/`instructionUrl`: владелец пришлёт ссылки отдельно позже.
- **Не синхронизировано с диском пользователя** (`~/Desktop/Development/MiniApp`
  либо где сейчас лежит копия) — правки внесены только через живой
  Apps Script редактор и веб-редактор GitHub. При следующей сессии с
  подключённым диском стоит подтянуть `git pull`, если бридж и права
  на папку это позволяют (см. эпизод про TCC/Desktop выше).

## Как редактировать backend (важно!)

Файлы в `backend-apps-script/*.gs` в этом гите — это зеркало/источник
правды для истории и ревью, но **сам живой код исполняется в отдельном
Apps Script проекте** (URL см. выше), и правки туда переносятся вручную
через браузер (Claude in Chrome / desktop bridge browser tools), а не
автоматическим деплоем из гита.

**Осторожно с `Config.gs`:** это единственный файл, где живая версия
исторически оказывалась НОВЕЕ репозитория, и на 03.09.2026 копии
совпадают по содержанию (тексты, `STAGE_IDS`), но расходятся в
комментариях — живая версия авторитетна. Не заменяй его целиком из
репозитория, правь точечно. Перед любой заменой файла в живом проекте
полезно построчно сравнить его с репозиторной копией и глазами
посмотреть, какие строки исчезнут: так уже находились комментарии,
которых в гите не было.

Рабочий процесс, который использовался
до сих пор:

1. Написать/поправить код сначала в файле в этом репозитории (проще
   проверить синтаксис — `node --check <файл>.js` после копии с
   расширением `.js`, `.gs`-синтаксис почти полностью совместим с
   обычным JS).
2. Закоммитить в гит.
3. Открыть тот же кусок кода в живом редакторе `script.google.com`
   (через браузерные инструменты) и внести те же изменения руками —
   печатать через `type()`, не копипастой (буфер обмена не всегда
   надёжен в автоматизации браузера).
4. **Обязательно визуально сверить результат** (скриншот/zoom) на
   лишние/пропавшие фигурные скобки перед сохранением — редактор Apps
   Script при автозакрытии скобок (когда печатаешь `{` и Enter) иногда
   дублирует закрывающую `}` в неожиданном месте, а иногда — нет,
   предсказать нельзя. Это происходило несколько раз именно в этом
   проекте и один раз почти привело к потере файла из-за незакрытого
   `if`. Через `Ctrl+A` + `Delete` над стандартным `function myFunction()
   {}` иногда не полностью очищает шаблон — тогда строка `function
   myFunction() {` остаётся первой строкой файла, а твой код
   вставляется ВНУТРЬ неё; тоже нужно визуально проверять первую
   строку файла после вставки, не только последнюю.
5. Сохранить (кнопка «Сохранить проект на Диск» либо `Ctrl+S`) и
   **перепроверить в свежей вкладке** того же URL проекта, что
   изменения реально сохранились на сервере — индикатор «Сохранено»/
   «Несохранённые изменения» в самой вкладке иногда не отражает
   реальность, если файл был структурно сломан в процессе редактирования.
6. Создать новый деплой версии (Начать развёртывание → Управление
   развёртываниями → редактировать активный деплой → Версия: «Новая
   версия» → Начать развёртывание) — без этого шага правки в редакторе
   не попадут в реальный `BACKEND_URL`, который обслуживает
   Telegram/amoCRM/фронтенд (Web App деплой — это застывший снимок кода,
   а не «живой HEAD»).
7. Имена функций, заканчивающиеся на `_` (например
   `adminListAllParticipants_`), Apps Script по конвенции прячет из
   выпадающего списка «Выполнить» — это нормально, они вызываются только
   из другого кода, не напрямую руками. Для ручного тестового запуска
   через UI нужна функция без `_` в конце.

## Тестирование / диагностика без пароля ADMIN_SECRET

Есть безопасный способ проверить, что роуты подключены, не зная секрет:
запрос с заведомо неверным `secret` должен вернуть чистый JSON
`{"error":"bad secret"}`, а не HTML-страницу с ошибкой сервера — если так,
значит код на сервере синтаксически цел и роут существует. Для более
глубокой проверки (Script Properties, чтение Sheets, вызов внутренних
`_`-функций) — удобно временно создать новый `.gs`-файл в живом
редакторе с диагностической функцией (имя без `_`, чтобы попало в
список «Выполнить»), прогнать её, посмотреть Журнал выполнения, потом
файл удалить (Меню файла → Удалить) — это не требует пересоздания
деплоя, если сам временный файл никогда не деплоился отдельно.

## Прочее

- Подробная инструкция «настроить с нуля» (если когда-нибудь понадобится
  поднять копию проекта на другом аккаунте Google/amoCRM) — `SETUP.md` в
  корне репозитория, части 1–5.
- Известные осознанные ограничения (квоты Apps Script, загрузка
  документов не сохраняет файл на диск, `changesRequiredVariant` не
  подключён к статусу) — раздел «Ограничения» в конце `SETUP.md`, не
  баги, а сознательные компромиссы ради простоты.

## Проверка живых данных (14.09.2026)

Первая проверка не кода, а самой боевой таблицы `WTUSA SELF` и пути
каждого значения от ячейки до экрана. Отчёт — `proverka-dannyh-14-09.md`,
ТЗ — `TZ-proverka-dannyh.md`.

**Кто реально пользуется приложением.** 327 строк в `Participants`,
Telegram подключён у 8, из них 5 — служебные (ClaudeTest, Oksana, Darya,
Zhansaya, офисный аккаунт), у всех пяти пустой `amo_deal_id`: заведены
руками в августе, вебхук их не трогает. Настоящих студентов **трое**:
`2023294062` (PLACEMENT_COMPLETED), `1020122206` (CIEE_REGISTRATION),
`1077767749` (CIEE_FILLED). Любые выводы «сколько людей это увидит»
считать от трёх, не от 327.

**Заголовки всех 12 листов сверены со схемой по позициям — совпадают.**
Это закрывает последствия ошибочного запуска `createSheetsIfMissing()` в
этот же день: ничего не сдвинулось. `Лист1` на месте (код ищет `Sheet1`).
В `Participants` 22 колонки — живая схема содержит
`job_offer_ready_confirmed`, которого нет в зеркале репозитория.

**Пустые листы и у кого нет писателя.** `VisaInfo` — 0 строк и **ни одной
строчки кода, которая в него пишет** (только чтение в `Api.gs:226`).
Из него берутся дата/время/место интервью, обратный отсчёт, статус
паспорта и статусы обоих визовых сборов. `Briefings` — 0, `SupportMessages`
— 0, `Coordinators` — 1 строка (Жансая) на 327 сделок.

**Дубли.** 41 `amo_deal_id` записан больше одного раза, 46 лишних строк;
ни одна копия не привязана к Telegram и не имеет этапа. Плюс 3 пустые
строки в `Payments` с ключами `:pay_1/2/3` (пустой telegram_id).
`telegram_id`, `pay_row_key`, `Events.id`, токены — дублей нет,
осиротевших строк нет, ссылки Events↔EventInvitations целы.

**Незакрытое: `ciee_escalated` не проставляется ни у кого.** Колонка пуста
у всех 64 участников на CIEE_REGISTRATION, при этом соседние отметки
(2d/1d/0d) стоят у всех 64 и пишутся тем же `updateRow` в том же цикле
`sendCieeActivationReminders_`. В журнале: 2 задачи координатору 10.09,
28 — 13.09, 30 — 14.09 (29 в запуске 07:57 + 1 в 14:20). Триггер
отработал без ошибок. Живой `Reminders.gs` совпадает с зеркалом, второй
копии функции в проекте нет (проверено по всем моделям monaco), вебхук
эту колонку не трогает. **Проверяется наблюдением:** если отметка не
работает, 15.09 задач будет около 60 (обе группы + новая); если около 1 —
колонку что-то стирает после записи. Журнал выполнения конкретного
запуска прочитать не удалось: список выполнений завален вебхуками
(~20 за 3 минуты), фильтр по функции в UI отсутствует, фильтры по типу
`?type=3`/`?type=9` дают пусто, статусы: `?status=3` — завершённые,
`?status=5` — сбои, `?status=2/4` — пусто.

**Актуальный деплой — Version 49** (в CLAUDE.md выше написано 37/39 —
устарело).

### Правки фронтенда, вышедшие из этой проверки

Прогон настоящих состояний трёх студентов через фронтенд (`real.mjs`)
нашёл пять мест, где показывалось число без источника — все исправлены
(`ed6fedb`, `09b4907`):

1. Главная печатала «Оплата 3 · срок уточняется · **$0**», а экран
   «Оплата» тот же платёж — «сумма уточняется».
2. `SELF 2027` в шапке (`app.js`) и в заголовке «Мой путь»
   (`roadmap.js`) — захардкоженный запасной вариант правки 03.09, который
   срабатывает ВСЕГДА, потому что `season` пуст у всех.
3. Профиль рисовал подписи «Программа»/«CIEE ID»/«Стоимость программы» с
   пустотой справа.
4. Профиль прогонял `programCost` через `formatMoney(null)` → «$0» —
   всплыло бы сразу после перевода стоимости в поле сделки.
5. Суммы визовых сборов не показывались никому никогда: статус берётся из
   пустого `VisaInfo`, бэкенд подставляет `locked`. Теперь до визового
   этапа — «откроется», после — сумма без выдуманной отметки об оплате.
6. Профиль показывал пустое имя координатора (экран «Документы» этот
   случай разбирал правильно, профиль — нет).

**Урок про выгрузку.** Наивный разбор CSV дал 215 строк в `EventLog`,
аккуратный — 151: переносы строк внутри текстов уведомлений ломали
разбор. Остальные 11 листов совпали. Любой подсчёт по выгрузке
перепроверять вторым способом.

**Не в коде, а в amoCRM:** у студента `1077767749` в поле третьего платежа
стоит `55555`, приложение честно показывает «$55 555».

## Деплой Version 50 и правки бэкенда (14.09.2026, вечер)

Идентификатор развёртывания и `BACKEND_URL` не изменились — обновлена
версия существующего Web App (Начать развертывание → Управление
развертываниями → карандаш → Версия: Новая версия). Подтверждено живым
трафиком: вебхуки amoCRM идут под «Версия: 50», все «Выполнение
завершено».

Что вошло:

- `Api.gs` — сбор SEVIS `220` → **`35`** (220 это F-1/M-1, а у нас J-1
  Summer Work Travel, ставка I-901).
- `Api.gs` — `programCost` берётся из `participantRow.program_cost`;
  `CFG_OPTIONAL("PROGRAM_COST_USD", 2850)` убран совсем. Пока поле в
  сделке не заведено, отдаётся `null`, и фронтенд блок не рисует.
- `Api.gs` — `cieeId` отдаёт `""` вместо «Будет добавлен после
  интеграции».
- `Webhooks.gs` — `program_cost: fieldGet("FIELD_ID_PROGRAM_COST") ||
  (previous && previous.program_cost) || ""` в `syncDealToSheets`.
- `Setup.gs` — `"program_cost"` в схеме `Participants`; прогнан
  `createSheetsIfMissing()`, колонка 23-я, на месте.
- `Setup.gs` — **удалена `wireUpSelfPipelineMapping2`** (19 записей без
  `SUBMIT_TO_CIEE`). Перед удалением сверено: у `wireUpSelfPipelineMapping`
  20 записей и `SUBMIT_TO_CIEE` есть, у `2` — 19 и нет.

Осталось до конца: в amoCRM завести поле «Стоимость программы, $» и
Script Property `FIELD_ID_PROGRAM_COST` (ТЗ — `tz-amocrm.md`). До этого
студенты стоимость не видят вообще — это сознательный выбор владельца.

### Как править живой код без риска сломать скобки

Печатать в редакторе Apps Script не нужно. Рабочий способ:

1. Найти модель: `monaco.editor.getModels().find(m => m.getValue().includes("<маркер>"))`
   — пути моделей выглядят как `/editor-1/file_7.js` и **с именами файлов
   не связаны**, ищи по содержимому.
2. Собрать новый текст в переменной, проверить: число вхождений старого
   куска строго 1, баланс `{}()[]` не изменился,
   `monaco.editor.getModelMarkers({resource: m.uri})` без severity 8.
3. Применить `m.pushEditOperations([], [{range: m.getFullModelRange(),
   text: newValue}], () => null)`.
4. **Кликнуть внутрь области редактора**, затем Ctrl+S. Без клика
   Ctrl+S не срабатывает, а кнопка «Сохранить проект на Диск» по клику
   не отработала ни разу.
5. Перезагрузить страницу и перечитать модель — только это доказывает,
   что сохранилось на сервере.

`javascript_tool` режет строки с `=`, `?`, `&` — передавай новый текст
**шестнадцатеричным кодом** и разворачивай на странице:
`new TextDecoder().decode(new Uint8Array(h.match(/../g).map(x=>parseInt(x,16))))`.

### Ловушка «Выполнить»: раскрыта

Выбор функции в выпадающем списке **не перепривязывает кнопку
«Выполнить» сразу**. Первый клик после смены выбора запускает
ПРЕДЫДУЩУЮ функцию, даже если в списке уже написано новое имя (проверил
скриншотом до клика — в списке стояло `listAmoPipelineStatuses`, а
выполнился `createSheetsIfMissing`). Второй клик запускает правильную.

Это и есть объяснение утренней ошибки 14.09 — не «не успел
примениться выбор», а именно такое поведение. **Правило: после смены
функции в списке всегда сверять по Журналу выполнения, ЧТО реально
запустилось, и при необходимости нажимать «Выполнить» второй раз.**

### Сверка статусов воронки закрыта

`listAmoPipelineStatuses()` прогнан. В «Сопровождение self» (id 9881242)
**26 статусов**, в карте — 20. Все 20 идентификаторов карты реально
существуют в воронке, устаревших нет.

Не сопоставлены шесть. Три из них так и задумано: «Неразобранное»,
«Успешно реализовано» и «Закрыто и не реализовано» (142/143 — общие для
всех воронок аккаунта).

**Три новых статуса заведены уже после 10.09 и приложению неизвестны**
(идентификаторы идут подряд — созданы одним заходом):

| Позиция | Статус | status_id |
|---|---|---|
| 6 | Обратная связь | 88549666 |
| 8 | Подать офер на проверку | 88549670 |
| 9 | Job Submitted to CIEE Oxana | 88549674 |

Сделка, переведённая в любой из них, для приложения «застревает» на
предыдущем этапе: уведомление не уходит, экран не меняется. Все три —
в блоке Job Offer, где студенты нервничают сильнее всего. Нужно решение
владельца: это внутренние статусы координаторов (тогда оставляем без
карты) или настоящие шаги, которые студент должен видеть (тогда
добавляем в `wireUpSelfPipelineMapping`, в `roadmap.config.js` и в
`STAGE_NOTIFY_TEXT`).

### Служебные файлы в живом проекте

Кроме одиннадцати рабочих `.gs` в проекте лежат `zzCheck2.gs` и
`без названия.gs` — следы прошлых диагностик. Не трогал.

## Три этапа Job Offer заведены целиком (14.09.2026, Version 51)

Решение владельца: все три неизвестных приложению статуса — настоящие
шаги, студент должен их видеть. Сделано во всех трёх местах.

| Статус в воронке | status_id | id этапа | order | Действие студента |
|---|---|---|---|---|
| Обратная связь | 88549666 | `JOB_SEARCH` | 4.5 | ищет работодателя |
| Подать офер на проверку | 88549670 | `JOB_OFFER_UPLOAD_CIEE` | 5.3 | **грузит офер в кабинет CIEE** |
| Job Submitted to CIEE Oxana | 88549674 | `JOB_OFFER_SENT_CIEE` | 5.6 | ничего |

- `roadmap.config.js` — три новых объекта, порядок **дробный** (как у
  `SUBMIT_TO_CIEE` 10.5 и `JOB_PROBLEM` 9.5), остальные этапы не
  перенумерованы. Всего этапов 23. Прогресс на новых: 25% / 32% / 35%.
- `Config.gs` — три записи в `STAGE_NOTIFY_TEXT` в том же сухом тоне.
  ⚠️ намеренно НЕ поставлен: он остаётся ровно на `JOB_PROBLEM` и
  `VISA_FINAL_CALL`; `JOB_OFFER_UPLOAD_CIEE` требует действия, но это
  рутинный шаг, как `JOB_OFFER_PARTICIPANT_REVIEW` — там ⚠️ тоже нет.
- `Setup.gs` — три записи в `wireUpSelfPipelineMapping`; функция
  прогнана, в журнале **«STATUS_ID_MAP_JSON set with 23 entries»**.
- Развёрнута **Version 51**, идентификатор развёртывания прежний.
  Подтверждено трафиком: вебхуки идут под «Версия: 51».
- Имя сотрудника из названия статуса («Oxana») в интерфейс не попало:
  этап назван по сути — «Офер отправлен в CIEE».

### Кнопка «Выполнить» привязана к ПЕРВОЙ функции файла

Утреннее объяснение («первый клик запускает предыдущую, второй —
нужную») **неверно**. Проверено шесть раз подряд: выбор в выпадающем
списке меняет подпись на кнопке, но запускается по-прежнему первая
функция открытого файла. Ни повторный клик, ни выбор стрелками с Enter,
ни клик мимо для потери фокуса не помогают. (Стрелки в этом меню ещё и
теряются: из пяти нажатий подряд доходит одно, жать по одному и сверять
подпись.)

**Рабочий способ запустить не первую функцию:** временно вставить в
самое начало файла обёртку

```js
function aaRunWireUpMapping() { wireUpSelfPipelineMapping(); }
```

сохранить, перезагрузить страницу, открыть файл (кликом по имени в
списке — после перезагрузки список инициализируется заново), убедиться
по подписи, что выбрана обёртка, нажать «Выполнить», потом обёртку
удалить. Так и был прогнан `wireUpSelfPipelineMapping` в этот раз.

Из-за этой ловушки `createSheetsIfMissing()` за вечер отработал шесть
раз вместо одного. Вреда нет — функция идемпотентна, пишет только первую
строку каждого листа, — но это ровно тот же механизм, что дал утренний
инцидент.

### Зеркало бэкенда в репозитории устарело сильнее прежнего

`backend-apps-script/*.gs` в гите **не отражает** живой код: там нет ни
правок Version 50 (SEVIS 35, стоимость из сделки, `program_cost`,
удалённая `wireUpSelfPipelineMapping2`), ни Version 51 (три этапа).
Живой проект авторитетен. Синхронизировать зеркало через браузер дорого:
вывод `javascript_tool` режется примерно на 950 символах, а файлы по
16–26 КБ. Нормальный путь — `clasp` с машины владельца.

## Сессия 22.09.2026 (вечер): оптимизация, интеграции с amoCRM, посещаемость

Полный пакет правок подготовлен и проверен (`node --check`, перекрёстные
ссылки), но **в живой проект НЕ применён**: защитный слой сессии блокировал
запись в Apps Script и загрузку на GitHub как «production deploy». Владелец
применяет сам по `КАК-ПРИМЕНИТЬ.md` (скрипт `apply-live-patch.js` в консоль
редактора → Ctrl+S → новая версия деплоя → два файла на GitHub). Пока это не
сделано — всё ниже в проде отсутствует; зеркало `backend-apps-script/*.gs`
в гите на этот раз НОВЕЕ живого кода (обратная ситуация к прежней).

Что уже сделано в проде руками из сессии (разрешили): в таблице добавлены
заголовки `Participants!X1 = first_name`, `EventInvitations!I1 =
attended_synced`; в `Events` у `evt_6fd53749` проставлен `briefing_key =
brief_1_intro`.

### Найдено

- **Глобальный замок в `syncDealToSheets`** (`LockService.getScriptLock`,
  30 с) сериализует ВСЕ синхронизации сделок; вебхуки, не дождавшиеся замка,
  молча пропускаются (`skipped: lock timeout`). Отсюда правило до фикса:
  **студентов добавлять пачками по 15–20 с паузой**, не всех разом. В патче
  `getDeal` и проверка воронки вынесены из-под замка (секция короче ~вдвое).
- **Ежедневные дубли задач «CIEE не активирован»** (2 → 28 → 30/день) —
  `Reminders.gs` ставил флаг `ciee_escalated` ПОСЛЕ HTTP в amoCRM без
  try/catch: 429 обрывал проход, флаг не писался никому. Та же схема в
  «платёж просрочен» дублировала бы сообщение студенту каждый день.
- `amoApiFetch_` не повторял 429/5xx; задачи вешались на владельца токена, а
  не на ответственного по сделке.
- `stateForUser_` читал пустой лист `Briefings` и писал `last_activity` на
  каждый запрос.
- Фронтенд (index/js/css) и `Sheets.gs` уже были выжаты досуха прошлыми
  сессиями — там менять нечего; медленно именно из-за платформы Apps Script
  (холодный старт 10–15 с) и вышеперечисленного.
- В `Participants` у служебных августовских строк `current_stage_id =
  CONTRACT_SIGNED` — старое имя `ENROLLED`; фронтенд падает на ENROLLED, не
  краш. В `ciee_escalated` у строк от 21.08 лежат даты — следы старой
  раскладки колонок, на логику не влияют.

### В патче (см. diff `changes-2026-09-22.patch`)

`AmoCRM.gs`: retry 429/5xx ×3 с паузой; `createCoordinatorTask(..,
responsibleUserId)`; `addDealNote_`; `contactFirstNameForDeal_`.
`Reminders.gs`: флаг до задачи + try/catch на участника + `flush()`; блоки
прогона независимы; `syncEventAttendanceToAmo_` (attended → поле
`FIELD_ID_BRIEFINGS_ATTENDED` + примечание, раз в сутки).
`Webhooks.gs`: HTTP вне замка; `first_name` из контакта (один GET, пока
пусто); `syncVisaInfoFromDeal_` → VisaInfo из `FIELD_ID_VISA_INTERVIEW_DATE`
(+TIME/LOCATION/RESULT).
`Api.gs`: `last_activity` раз в 15 мин; `Briefings` не читается;
`firstName` в state; поддержка → задача (4 ч) + примечание; загрузка/
запись/отказ/«готов к визе» → примечания (`noteToDeal_`); роут `autoLink`
(GET и POST, `WEBHOOK_SECRET`) — Salesbot/вебхук на этап отправляет ссылку
привязки в WhatsApp сам; роуты `adminListInvitations`/`adminSetAttendance`.
`Events.gs`: примечания при ответе на приглашение; `adminListInvitations_`,
`adminSetAttendance_`. `Setup.gs`/`Config.gs`: схема и документация ключей.
Фронтенд: `home.js` — «Добрый день, Имя» из `participant.firstName`;
`admin-events.html` — вкладка «Посещаемость»; `events.js` — прошедшее
мероприятие без отметки показывается как «прошло», а не «Вы записаны».

### После применения владельцем

1. Проверить роуты «плохим секретом» (см. `ПРОВЕРКА-ПОСЛЕ-ДЕПЛОЯ.md`).
2. Завести в amoCRM поля: «Дата визового интервью» (дата), «Посещённые
   брифинги» (текст), опц. время/место/результат; ID → Script Properties
   `FIELD_ID_VISA_INTERVIEW_DATE`, `FIELD_ID_BRIEFINGS_ATTENDED`, ...
3. Вебхук/Salesbot: `{BACKEND_URL}?action=autoLink&secret={WEBHOOK_SECRET}&dealId={{lead.id}}`.
4. Решить: `Events.roadmap_stage_id = CONTRACT_SIGNED` → `ENROLLED` или
   очистить; `attended` для 1077767749 за 23.08 (через вкладку
   «Посещаемость»).

### Визуальный проход 22.09 (поздний вечер)
Прогнал все экраны на демо-данных (Playwright, 390×844). Убраны эмодзи из
интерфейса: маркеры этапов на «Пути» (✓/🔵/✅ → рисованные SVG: галочка,
точка-с-кольцом, пусто), 🟢🔴 в заголовках карточек → цветная точка,
📅🕐📍👥 на «Событиях» → SVG, 💬 в комментариях → SVG, «Подключён ✅» → точка.
Нижний ряд — цветные двухтоновые SVG, 27px. Главная кнопка этапа показывается
только на ТЕКУЩЕМ этапе (раньше и на пройденных/будущих). Плашка «Позже» без
класса получила фон. Профиль: «Program/Season» → «Программа/Сезон».
Празднования (🎉 🇺🇸 ✈️ в celebration.js/чек-листе) оставлены намеренно.

### Вечер 22.09, вторая волна (в том же патче)
- Курс НБ РК (`nbkUsdRate_` в Api.gs, фид rates_all.xml, кеш 6 ч) → `state.fxRate`;
  экран «Оплата» показывает ≈ сумму в тенге и ссылку-строку на курсы.
- `stageDatesFor_`/`stageDatesForAll_` (Events.gs, из EventLog) → даты на «Пути»
  и «на этапе N дней» в admin-users (фильтры: застряли 14+, просрочка, не заходили).
- Reminders.gs: брифинг завтра, интервью −3/−1 день, документ на исправлении 3+ дня —
  дедуп через EventLog (`reminder_sent` + ключ), без новых колонок.
- Webhooks.gs: push при новом комментарии координатора; паспорт/сборы из полей
  `FIELD_ID_PASSPORT_STATUS`, `FIELD_ID_SEVIS_PAID`, `FIELD_ID_VISA_FEE_PAID`.
- Главная: «Ближайшее» берёт брифинг из реальных приглашений, не из пустого Briefings.
- Убраны из схемы `Briefings` и `SupportMessages` (вкладки в таблице удалить руками).
- `admin-panel.html` — один вход, три вкладки (фреймы старых страниц).
- «Что означает статус?» → «Подробнее об этапе»: раскрыт на текущем, дубли описания скрыты.
- Договорённости: загрузка документов — в существующие папки Drive сезона (привязка по
  имени, `drive_folder_id`), студент Drive не видит; 5 служебных строк Participants —
  удалить; имя/фамилия — из контакта amoCRM (`full_name`).

## Сессия 23.09.2026: всё из 22.09 применено в продакшен

- **Apps Script, версия 56** (23.09, 12:37). Все 7 файлов патчились в живом редакторе через monaco `pushEditOperations`, каждая функция сверена SHA-256 с `backend-apps-script/*.gs`. Расхождения живого кода с зеркалом, обнаруженные по пути: в `sendScheduledReminders` живой код вызывал ещё `runDailyHealthCheck()` — оставлен, добавлен в список блоков; в `syncDealToSheets` между `coordinator` и `upsertRow` есть блок авто-даты 3-го платежа (15.09) — не тронут; `confirmVisaReady_` и `case "confirmVisaReady"` в живом коде с другим отступом. Зеркало в `backend-apps-script/` теперь соответствует живому по всем правленым функциям.
- `backfillMissingDeals`: параметр фильтра amoCRM исправлен на `filter[pipeline_id][0]=` (`filter[pipelines]` не фильтровал вовсе). Первый прогон: создано 67, пропущено 23, ошибок 0, остановлен по 5-минутному лимиту; второй прогон — см. журнал выполнений. 16 сделок со статусом 142 («успешно реализовано») попадают без этапа: `No STAGE_IDS mapping for status_id 142`.
- Вебхук amoCRM пересоздан `fixAmoWebhookUrl` → id 48772206, активен, подписан на все события (нелид-события отбрасываются в `handleAmoWebhook` без обращения к Sheets).
- Из `Setup.gs` удалена мёртвая `seedExampleBriefings` (писала в удалённый лист). Временные обёртки `aaFix`/`aaBackfill` в начале `Setup.gs` — удалить после досинхронизации.
- Фронтенд загружен на GitHub 7 коммитами «23.09 (n/7)» через страницу Upload по папкам; хеши raw-файлов совпадают с локальными.
- Урок по инструментам: JS-вызовы в редактор > ~20 КБ роняют вкладку; Chrome в фоне (`visibilityState=hidden`, окно 0×0) — редактор не сохраняет и не запускает функции. Сохранять пофайлово.
- 23.09 (вечер): приветствие на главной снова без имени — решение владельца (имена в amoCRM вбиты вручную на русском/казахском, с ошибками). `fullName/firstName` в state остаются для кабинета и уведомлений.
- 23.09 (вечер), **версия 57**: закрытые сделки (142 «Успешно реализовано», 143 «Закрыто и не реализовано») больше не заводят строк в Participants — `isClosedAmoStatus_` + ранний выход в `syncDealToSheets` (решение владельца: это бывшие участники). Уже привязанные к Telegram строки при закрытии сделки продолжают синхронизироваться. Одноразовой `aaRemoveClosed` из Participants удалено 87 строк закрытых сделок без telegram_id (проверено 217 строк без этапа) — среди них не только 16 сегодняшних, но и старые строки прошлых синков.
- 23.09 (вечер), подготовлено к версии 58 (ещё не применено в живой проект): (а) текст приглашения `autoLinkMessage_` в Api.gs и тот же текст в admin.html — формулировка владельца, без имени; (б) `bulkAutoLink(dryRun, max)` в Setup.gs — запасной массовый вариант, не запускать без владельца; (в) **мониторинг ошибок**: `reportError_` / `reportApiError_` / `clientError_` в Api.gs, хуки в `handleAmoWebhook` и `sendScheduledReminders`, фронт — `window.error`/`unhandledrejection` в app.js и `__reportScreenError` в router.js → роут `clientError`. Ошибки уходят в Telegram-чат из Script Property `ADMIN_TELEGRAM_ID` (дедуп 1 ч, ≤15/сутки), всегда — в EventLog (source=monitor). Ожидаемые отказы (bad secret, UNAUTHORIZED, просроченная ссылка, OFFLINE/TIMEOUT) не репортятся. Решение владельца: автопривязка — через Salesbot с ручным запуском (по одной сделке или по выбранным в списке), без триггера на этап; автотриггер на «Оформились» — позже.
- 23.09 (вечер): **баг** — кнопка «У меня есть Job Offer» на ГЛАВНОЙ (карточка «Сейчас») оставалась активной после нажатия: 22.09 флаг `ctaDone` подключили только к экрану этапа. Исправлено: `deriveActionCard` отдаёт `ctaDone` (общий `ctaAlreadyDone_`), home.js рисует серую «✓ Подтверждено — передано координатору» и блокирует обе одноразовые кнопки сразу при нажатии.
- 23.09 (ночь), полный прогон перед версией 58: синтаксис всех js/gs, разрешение импортов и именованных экспортов, сверка действий фронта/админок с роутами бэкенда, колонок записи со схемой, этапов фронт↔бэкенд; headless-прогон (Playwright, демо-режим) всех 6 экранов и 23 экранов этапов — ошибок JS нет, шрифт везде Inter; VM-тест `reportError_`/`reportApiError_`/`clientError_` (дедуп, лимит 15/сутки, UNAUTHORIZED не репортится). Найдено и исправлено: (1) «Этап пройден» подписывались и БУДУЩИЕ этапы на экране этапа → теперь «Предстоит» (statusDetail.js, detail.status); (2) «Оплачено $0» при оплаченном первом платеже в тенге → наверху экрана «Оплата» суммы по валютам + «1 из 3» (deriveViews/payments.js); (3) четыре этапа без своего текста уведомления в STAGE_NOTIFY_TEXT (Config.gs). Прогон: scratchpad/e2e/run.js, run2.js (одноразовая кнопка), run4.js (админки), gs-test.js.

## Сессия 24.09.2026

- **Версия 58** (12:03): применено то, что было подготовлено 23.09 вечером — текст приглашения `autoLinkMessage_` (без имени, с оговоркой про первую версию приложения и просьбой давать фидбек — формулировка владельца), мониторинг ошибок (`reportError_`/`reportApiError_`/`clientError_`), `STAGE_NOTIFY_TEXT` для 4 этапов, `bulkAutoLink` (запасной, не запускать без владельца).
- **Баг**: кнопка «У меня есть Job Offer» на главной снова оставалась активной после нажатия — `ctaDone` был подключён только к экрану этапа, не к карточке действия на home.js (сама карточка `actionBlockHtml` рендерилась без учёта `action.ctaDone`). Исправлено вместе с 58.
- Решение владельца: автопривязку к приложению делать через отдельный этап пайплайна «Отправить ссылку в приложение» (создан между «Оформились» и «Регистрация в CIEE», id `88848478`), а не через Salesbot с ручным запуском — на этом тарифе amoCRM у Salesbot нет кнопки ручного запуска ни в карточке, ни массово. Salesbot «Автопривязка к приложению» (создан 24.09) остался без триггера — не используется.
- **Версия 59** (12:38): восстановлено поведение, которое я по ошибке убрал 23.09 как «дубль» — `confirmJobOffer_` при нажатии студентом «У меня есть Job Offer» снова сам переводит сделку на этап «Обратная связь» (`updateDealStatus_`, в try/catch, с текстом задачи под оба исхода). Добавлено логирование ключей запроса в GET/POST `autoLink` — для диагностики параметров, которые реально шлёт amoCRM.
- **Версия 60** (12:47), **61** (12:56): автопривязка по этапу — оказалось, что Salesbot-триггер на этап не годится (не виден в логах, не запускается без контакта, нельзя проверить), поэтому автопривязку делает сам вебхук: `handleAmoWebhook` (Webhooks.gs) при любом событии сделки (`status`/`add`/`update`) со `status_id` = Script Property `AUTOLINK_STATUS_ID` (по умолчанию `88848478`) сам вызывает `autoLinkDeal_`. Защита от повторной отправки — по наличию строки в `LinkTokens` для сделки (не только «живая» ссылка, как раньше в `autoLinkDeal_`, а вообще любая когда-либо выписанная): если ссылка уже когда-то отправлялась, повторно её не шлём даже после истечения — повторную отправку делает координатор вручную через admin.html.
- Сквозная проверка (реальный вебхук amoCRM, не симуляция): тестовая сделка без телефона на контакте → при попадании на «Отправить ссылку в приложение» вебхук вызвал `autoLinkDeal_`, получил `no phone on contact`, поставил координатору задачу «отправьте ссылку вручную через admin.html» — задача не задублировалась при повторном попадании сделки на тот же этап. Тестовая сделка закрыта статусом «Закрыто и не реализовано» (143) после проверки. Основная тестовая сделка (Андрей Кишкин, 48655641, id 48655641) возвращена на «Оформились».
- Открыто: `AUTOLINK_STATUS_ID` в Script Properties не заведён явно — используется дефолт `88848478` из кода; если этап когда-нибудь пересоздадут с другим id, обновить свойство. Новый этап не размечен в `STATUS_ID_MAP_JSON` → `syncDealToSheets` пишет в лог «No STAGE_IDS mapping», это ожидаемо и безвредно (этап чисто технический, на фронте его быть не должно). Всё ещё нужен Telegram chat_id владельца для `ADMIN_TELEGRAM_ID`.


### `AbcUniverseSync.gs` (отдельная таблица «2027 ABC Universe», не Mini App) — 24.09.2026

- **Урок**: Google Sheets отклоняет `setValue()` значением вне списка валидации (дропдаун) исключением, которое без `SpreadsheetApp.flush()` сразу после каждой записи не всплывает синхронно и не ловится — а без try/catch вокруг каждой отдельной записи одно расхождение справочника останавливало весь прогон (включая необработанные строки, «Документы», автоперенос в «Отказ» и сверку). Исправлено: каждая запись в `abcSyncStatusesAndPayments_` обёрнута в try/catch с `flush()` сразу после, отклонённые ячейки собираются в `failedUpdates` и не блокируют остальное.
- **Диагностика «Андрей»**: одноразовая `abcListResponsibleNames()` (без завершающего `_` — иначе функция не появляется в ручном Run-дропдауне Apps Script, это отдельно обнаруженный факт) показала, что в воронке self за ответственными числится не только Айжан/Жансая (35/171), но и Андрей — 18 сделок (13 из них ловились в `failedUpdates`, т.к. «Андрей» нет в списке валидации колонки «Ответственный»). Владелец подтвердил: self ведут только Айжан и Жансая; Андрей — воронка «Фулл», её не трогаем; Алуа тоже относится к «Фулл», не к self. Решение: список валидации и логику синхронизации оставить как есть («Оставить как есть») — расхождения продолжают попадать в `failedUpdates` и пропускаются, а не «чинятся» молча. Владелец сам переназначил Андрея → Айжан на 13 сделках в CRM напрямую; повторный прогон подтвердил 0 расхождений на «Андрее».
- **Баг инструмента (Run-дропдown, повторяется)**: выбор функции в дропдауне «Выполнить» и немедленный клик «Выполнить» иногда запускает ПРЕДЫДУЩУЮ выбранную функцию, а не только что выбранную — визуальная метка на тулбаре при этом может показывать правильное имя, но реально выполняется другая функция (подтверждено дважды по несовпадению лейбла и записи в «Журнал выполнения»). Обнаружено, что клик прямо в редактор кода после выбора функции (для «подтверждения»/снятия фокуса с дропдауна) сам по себе может сбросить выбор на функцию, в которую попал курсор (если клик пришёлся на код внутри другой функции) — поэтому подтверждать выбор нужно кликом в дропдаун-список ещё раз (или пустое место тулбара), а не кликом в код. Единственная надёжная проверка — сверить реальный текст записи в «Журнале выполнения» после прогона (не только метку на тулбаре).
- **Новая функция — реверс-синхронизация «таблица → amoCRM», ОДИН конкретный переход** (по явной просьбе и подтверждению владельца, 4 условия): владелец согласился со следующей логикой на замену ручному подтверждению по почте — Оксана в «Студенты» ставит статус «Передан в CIEE», когда сделка в CRM сейчас на статусе «Job Submitted to CIEE Oxana» (id `88549674`, воронка self) — тогда `abcPushOxanaCieeTransition_` переставляет сделку в CRM на статус «Передан в CIEE» (id `88370982`). Условия: (1) CRM всегда главнее таблицы; (2) из таблицы можно менять только статус, больше ничего; (3) разрешён только этот один переход; (4) любое другое ручное изменение статуса в таблице просто перезатирается следующим `abcSyncStatusesAndPayments_` значением из CRM — отдельной проверки конфликтов не нужно, это гарантируется одним лишь порядком вызова (`abcPushOxanaCieeTransition_` вызывается в `abcDailySync` ПЕРВОЙ, до pull-синхронизации). Id статусов получены разовой диагностикой `abcListPipelineStatuses()` (удалена после использования). Сопоставление сделки — по телефону, как и везде в файле; неоднозначные совпадения (несколько сделок на один телефон) пропускаются по той же причине, что и в `abcSyncStatusesAndPayments_`.
- Протестировано `abcDryRunTest()` (dryRun, без записи) — 0 неожиданных срабатываний push, 0 ошибок; затем `abcDailySync()` вживую — 0 push (нет сделок в нужном статусе на момент прогона), 0 status updates (все уже синхронизированы), 64 doc updates, 1 ambiguous skipped, 0 failed, 0 moved в «Отказ», сверка: 31 нет в CRM / 50 нет в таблице / 0 расхождений статуса.
- Добавлен ежедневный триггер `abcDailySync` (по времени, «По дням», окно 0:00–1:00 GMT+5) — второй триггер проекта после `sendScheduledReminders`.
- Открыто: email-подтверждение, о котором говорил владелец («без подтверждения через почту итд»), не относится к коду этого проекта — в `backend-apps-script/*.gs` нет отправки почты вообще (грепом не найдено ни одного `MailApp`/`GmailApp`). Это либо ручной процесс вне amoCRM/Sheets, либо настройка в самой amoCRM (автоматизация/Salesbot) — если владелец хочет буквально убрать какую-то отправку письма, нужно уточнить, где именно она настроена (это не в зоне Apps Script). Смысл в её отмене после этой фичи есть: раз статус теперь меняется напрямую из таблицы в CRM, необходимость в промежуточном подтверждении по почте отпадает.

- **Поправка к пункту выше «Открыто: email-подтверждение… не относится к коду этого проекта»** — это неверно, исправлено тем же вечером (см. раздел ниже). Я грепал зеркало `backend-apps-script/`, а письмо Оксане живёт только в ЖИВОМ проекте («director review mini-flow», в зеркале его нет — об этом прямо сказано в комментарии `Config.gs` строки ~177).

### Письмо Оксане, отключённый вебхук и очередь вебхуков — 24.09.2026 (вечер)

- **Где живёт письмо Оксане (нашлось в живом коде, НЕ в зеркале).** Когда сделка входит в «Подать офер на проверку» (`STATUS_ID_PODAT_OFFER_NA_PROVERKU` = 88549670), `syncDealToSheets` (Webhooks.gs, ~стр. 145–163 живого файла) вызывает `notifyOksanaForOfferReview_` (AmoCRM.gs, ~стр. 253–290): WhatsApp на `OKSANA_WHATSAPP_PHONE` + письмо `MailApp.sendEmail` на `OKSANA_EMAIL` (оба в Config.gs, ~стр. 118–132) с одноразовой ссылкой (`createOksanaConfirmToken_`/`consumeOksanaConfirmToken_` в Auth.gs ~стр. 242–280). Клик → `doGet` action `confirmCiee` (Api.gs ~стр. 72) → сделка в `STATUS_ID_OTPRAVLENO_CIEE_OKSANA` = 88549674 «Job Submitted to CIEE Oxana». Дедуп — CacheService 300 с. Проверка 11.09 шла тестовой сделкой «ТЕСТ — можно удалить (Oksana notify e2e)» (48480199, удалена). **Весь этот поток запускается вебхуком amoCRM** — пока вебхук выключен, Оксане ничего не уходит. В самой amoCRM (все 9 воронок, все Salesbot-ы, вебхуки, интеграции amoМаркета) писем Оксане нет — проверено.
- Это ДВА разных шага, не дубль: письмо двигает «Подать офер на проверку» → «Job Submitted to CIEE Oxana»; таблица ABC Universe (`abcPushOxanaCieeTransition_`) — «Job Submitted to CIEE Oxana» → «Передан в CIEE». Владелец решил оставить оба (нагрузка ничтожная).
- **ЗЕРКАЛО НЕПОЛНОЕ.** Oksana-flow (и, возможно, ещё что-то из «сентября, не в зеркале») есть только в живом проекте. Прежде чем утверждать «такого в коде нет» — искать в живом редакторе (monaco `getModels()`), а не грепом по репо.
- **Вебхук amoCRM был отключён** («Webhook отключен из-за невалидного отклика»). Последний принятый doPost — 24.09 12:57; вечерние правки сделок (переназначение Андрей → Айжан) до скрипта уже не доходили. Причина достоверно не установлена: все дошедшие вызовы завершались без ошибок, но ответ doPost был синхронным (обработка сделки до ответа, 0,6–8 с) — вероятный повод для amoCRM считать ответ сбоем. Кнопка «Включить» в amoМаркет → Web Hooks + «Сохранить» **не сохранилась** (API продолжал показывать `disabled:true`) — надёжно включается только повторной подпиской через API.
- **Грабли API amoCRM:** `GET /api/v4/webhooks` отдаёт `destination` с HTML-экранированием (`&amp;secret=`), а хранит правильный `&secret=`. Если переподписать, скопировав `destination` из ответа как есть, amoCRM создаст ОТДЕЛЬНУЮ подписку с буквальным `&amp;secret` — скрипт получает параметр `amp;secret`, `e.parameter.secret` пуст → `bad secret` (200, amoCRM не видит проблемы, события молча теряются). Так и случилось в 23:13–23:28 (подписка 48780714, удалена). Правильно: `destination.replace(/&amp;/g, "&")` → POST; amoCRM узнаёт существующую подписку (48772206) и включает её.
- **Очередь вебхуков (версии 62–63, файл `WebhookQueue.gs`, по решению владельца).** `doPost` action `amoWebhook` теперь вызывает `enqueueAmoWebhook_(e.parameter)`: проверка secret → параметры (без secret/action) в Script Property `WHQ_<ms>_<rand>` → мгновенный ответ. Триггер `processAmoWebhookQueue` раз в минуту разбирает очередь и зовёт тот же `handleAmoWebhook(params)` (логика обработки не менялась, задержка ≤ ~1 мин). Замок — `getUserLock` (НЕ `getScriptLock`: его держат `syncDealToSheets`/токен amoCRM). Событие > 8500 символов обрабатывается сразу по-старому. `doGet` action `amoWebhook` не тронут (amoCRM шлёт POST).
- Самодиагностика: журналы Cloud Console для запусков по триггеру/вебу в этом проекте НЕ отдаются («Журналы недоступны»), поэтому `WHQSTAT_ENQ` (последняя постановка) и `WHQSTAT_RUN` (последний непустой разбор: done/failed/queued) пишутся в Script Properties — смотреть в «Настройки проекта → Свойства скрипта». `whqStatus()` — сколько ждёт.
- Проверено вживую: самотест из редактора (doPost с верным secret → `{"ok":true,"queued":true}`, с `amp;secret` → `bad secret`, временная функция удалена); реальное событие amoCRM по тестовой сделке «Андрей Кишкин» (48655641, тег `webhook-queue-test` ставился/снимался, сделка возвращена как была) → WHQSTAT_ENQ 23:29:44; разбор 23:30:44 — done=6 failed=0 (пошли и реальные сделки), очередь пуста. Триггеров теперь 3: `sendScheduledReminders`, `abcDailySync`, `processAmoWebhookQueue` (1 мин).
- **Инцидент с секретом:** при диагностике значение `WEBHOOK_SECRET` попало в вывод сессии (маскировал ключ `secret`, а в экранированном адресе он назывался `amp;secret`). Рекомендовано владельцу сменить `WEBHOOK_SECRET` (Script Property + адрес подписки amoCRM + всё, что зовёт `autoLink` с этим секретом) — ждёт решения.
- Инструменты: `GET/PATCH/POST /api/v4/...` amoCRM работают прямо из вкладки amoCRM через `fetch(..., {credentials: "include", headers: {"X-Requested-With": "XMLHttpRequest"}})` — без токена, по сессии владельца. javascript_tool не выполняется на странице с «сырым» JSON (открывать любую обычную страницу amoCRM). Вывод javascript_tool режется ~1000 символами и блокируется при похожести на секреты/base64 — код читать сигнатурами, править через `model.pushEditOperations` и сверять SHA-256 с локальным файлом.
- 23:45, решения владельца: (1) триггер `processAmoWebhookQueue` переведён с «раз в минуту» на **«раз в 5 минут»** — экономия квоты фоновых запусков (бесплатный аккаунт: 90 мин/сутки; раз в минуту съедало ~30 мин), задержка обработки событий amoCRM теперь до ~5 мин. Комментарии в `WebhookQueue.gs` («раз в минуту») не правились — ориентироваться на триггер. (2) Задан `ADMIN_TELEGRAM_ID = 1077767749` (Telegram владельца @andreikishkin — тот же id привязан к его тестовой сделке 48655641 и уже стоял в `ALERT_TELEGRAM_CHAT_ID` для ежедневной проверки здоровья). Теперь оповещения мониторинга ошибок (`reportError_`/`reportApiError_`/`clientError_`) идут ему в Telegram. Свойство ставилось точечно временной функцией (удалена), а НЕ через форму «Изменить свойства скрипта» — форма сохраняет весь набор свойств разом и может потерять/задублировать события очереди `WHQ_*`.
